name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: '版本号 (例如: v2.0.0)'
        required: true
        type: string

env:
  GO_VERSION: '1.24' # 已根据你的环境更新到 1.24
  BINARY_NAME: phantom-server

permissions:
  contents: write

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - name: 确定版本
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

  build:
    name: Build (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: linux
            goarch: arm
            goarm: 7
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
            ext: .exe
          - goos: freebsd
            goarch: amd64

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: 下载依赖
        run: go mod download

      - name: 构建
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
          CGO_ENABLED: 0
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          COMMIT=$(git rev-parse --short HEAD)
          BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S')
          
          ARCH_SUFFIX="${{ matrix.goarch }}"
          [[ "${{ matrix.goarch }}" == "arm" ]] && ARCH_SUFFIX="armv${{ matrix.goarm }}"
          
          BINARY="${{ env.BINARY_NAME }}-${{ matrix.goos }}-${ARCH_SUFFIX}${{ matrix.ext }}"
          
          go build -trimpath \
            -ldflags "-s -w \
              -X 'main.Version=${VERSION}' \
              -X 'main.BuildTime=${BUILD_TIME}' \
              -X 'main.GitCommit=${COMMIT}'" \
            -o "${BINARY}" \
            ./cmd/phantom-server
          
          echo "BINARY=${BINARY}" >> $GITHUB_ENV
          echo "ARCH_SUFFIX=${ARCH_SUFFIX}" >> $GITHUB_ENV

      - name: 打包
        run: |
          ARCHIVE_NAME="${{ env.BINARY_NAME }}-${{ matrix.goos }}-${{ env.ARCH_SUFFIX }}"
          
          mkdir -p package
          cp "${{ env.BINARY }}" package/
          # 检查文件是否存在再拷贝，避免报错
          if [ -f configs/config.example.yaml ]; then cp configs/config.example.yaml package/config.yaml; fi
          if [ -f README.md ]; then cp README.md package/; fi
          if [ -f LICENSE ]; then cp LICENSE package/; fi
          
          if [[ "${{ matrix.goos }}" == "windows" ]]; then
            FILE_NAME="${ARCHIVE_NAME}.zip"
            cd package && zip -r "../$FILE_NAME" . && cd ..
          else
            FILE_NAME="${ARCHIVE_NAME}.tar.gz"
            tar -czvf "$FILE_NAME" -C package .
          fi
          
          # 修正这里：直接使用 Shell 变量 FILE_NAME，而不是 GitHub 上下文
          sha256sum "$FILE_NAME" > "$FILE_NAME.sha256"
          
          # 传递给后续步骤
          echo "ARCHIVE=$FILE_NAME" >> $GITHUB_ENV

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            *.tar.gz
            *.zip
            *.sha256
          retention-days: 1

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [prepare, build]
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 下载产物
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: release-*

      - name: 整理文件
        run: |
          mkdir -p release
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" -o -name "*.sha256" \) -exec mv {} release/ \;
          cd release && cat *.sha256 > checksums.txt
          ls -la

      - name: 生成发布说明
        run: |
          TAG="${{ needs.prepare.outputs.tag }}"
          cat > release-notes.md << EOF
          # 🚀 Phantom Server ${TAG}
          
          ## ✨ 特性
          
          - 🎯 **极简设计** - 约 600 行核心代码
          - 🔒 **TSKD 认证** - 时间同步密钥派生，0-RTT
          - 🛡️ **ChaCha20-Poly1305** - 高性能 AEAD 加密
          - 👻 **抗探测** - 全密文传输，静默丢弃
          
          ## 📦 下载
          
          | 平台 | 架构 | 下载 |
          |------|------|------|
          | Linux | amd64 | [phantom-server-linux-amd64.tar.gz](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-linux-amd64.tar.gz) |
          | Linux | arm64 | [phantom-server-linux-arm64.tar.gz](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-linux-arm64.tar.gz) |
          | Linux | armv7 | [phantom-server-linux-armv7.tar.gz](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-linux-armv7.tar.gz) |
          | macOS | amd64 | [phantom-server-darwin-amd64.tar.gz](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-darwin-amd64.tar.gz) |
          | macOS | arm64 | [phantom-server-darwin-arm64.tar.gz](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-darwin-arm64.tar.gz) |
          | Windows | amd64 | [phantom-server-windows-amd64.zip](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-windows-amd64.zip) |
          | FreeBSD | amd64 | [phantom-server-freebsd-amd64.tar.gz](https://github.com/${{ github.repository }}/releases/download/${TAG}/phantom-server-freebsd-amd64.tar.gz) |
          
          ## 📋 校验和
          
          <details>
          <summary>SHA256</summary>
          
          \`\`\`
          $(cat release/checksums.txt)
          \`\`\`
          
          </details>
          EOF

      - name: 创建 Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: Phantom Server ${{ needs.prepare.outputs.tag }}
          body_path: release-notes.md
          draft: false
          prerelease: ${{ contains(needs.prepare.outputs.tag, 'alpha') || contains(needs.prepare.outputs.tag, 'beta') }}
          files: |
            release/*.tar.gz
            release/*.zip
            release/*.sha256
            release/checksums.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}